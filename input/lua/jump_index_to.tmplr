title: jump_index_to()
brief: Jump to a position in the message list.
seealso: scroll_index_down, scroll_index_up, scroll_index_to, index_offset
version: 0.07
----

<p>When a folder has been opened this function causes the list of messages to be scrolled to the given offset.</P>
<p>For example:</p>
<pre>
-- Jump to the end of the list:
function index_end()
   jump_index_to( <a href="/lua/count_messages.html">count_messages</a>() - 1 );
end
</pre>

