title: cd()
brief: Change the current working directory.
seealso: cwd
version: 0.25
----

<p>This function will change the current working directory which is used for all relative file-accesses.</p>
<p>A simple example would be:</p>
<pre>
cd( os.getenv( "HOME" ) )
</pre>
